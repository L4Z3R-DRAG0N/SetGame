def set_solver(cards):
    set = [[] for j in range(4)]
    for i in range(12):
        attributeCount = 0
        for j in range(-11):
            if cards[i][0]==cards[j][0]
                
                
                
                
                attributeCount += 1
        
        if attributeCount == 4:
            set[].append()